# План создания биллинг-системы на Hono (Cloudflare Workers)

Проект представляет собой систему перепродажи API (STT, LLM, Image Gen) для видеоредактора Fastcat.

## 1. Подготовка инфраструктуры
- [x] Инициализировать Hono в папке `server` с шаблоном `cloudflare-workers`.
- [x] Добавить зависимости: `@hono/zod-validator`, `zod`.
- [x] Настроить `wrangler.toml` (wrangler.jsonc) для работы с D1 и KV.

## 2. Схема базы данных (D1)
- [x] Создать таблицы: users, services, usage_logs, transactions. (см. src/db/schema.sql)

## 3. Разработка API
- [x] Middleware для аутентификации по API-ключам (в index.ts).
- [x] Роут `POST /usage`: фиксация использования ресурсов и списание баланса.
- [x] Роут `GET /balance`: проверка остатка.
- [ ] Роут `POST /proxy/*`: проксирование запросов (оставлено для следующего этапа).

## 4. Безопасность и масштабируемость
- [x] Использование UUID для идентификаторов транзакций.
- [ ] Кэширование балансов в KV.

## 5. Документация
- [ ] Описать формат API для интеграции с fastcat-ui.
